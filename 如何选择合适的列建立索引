- 高频出现在WHERE子句、JOIN条件以及ORDER BY、GROUP BY语句里的列，是创建索引的优先选择。
- 数据区分度指的是列中不同值的数量与总行数的比例。高区分度的列（像主键、用户 ID）适合建立索引，而低区分度的列（例如性别、状态）单独建索引的效果可能不太好。
- 避免对更新频繁的列建立索引
- 可以借助数据库提供的查询分析工具（如EXPLAIN语句）来查看查询执行计划，从而找出潜在的索引优化点。
