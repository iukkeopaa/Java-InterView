1. 算法开始时，为每个服务器分配?个**初始权重**，通常情况下，权?值可以根据服务器的

性能、处理能?、连接数等因素进?设置。权?值越?，表示该服务器可以处理更多的

请求。

2. 当有新的请求到达时，负载均衡器会选择具有最?权?的服务器处理该请求。

3. 选定服务器处理请求后，该服务器的权?会减去?个事先设定好的值，以降低其权?，

使得在下?次轮询中被选中的可能性减?。

4. 当所有服务器的权?值都减为 0 时，它们将?新被赋予初始权?值，然后再次参与请求

的分发。

5. 复上述步骤，直到服务终?或者负载均衡器收到终?信号